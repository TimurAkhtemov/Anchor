
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'anchor'
version: '1.0.0'
config-version: 2

# This setting configures which "profile" dbt uses for this project.
profile: 'anchor'

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# In dbt, the default materialization for a model is a view. This means, when you run 
# dbt run or dbt build, all of your models will be built as a view in your data platform. 
# The configuration below will override this setting for models in the example folder to 
# instead be materialized as tables. Any models you add to the root of the models folder will 
# continue to be built as views. These settings can be overridden in the individual model files
# using the `{{ config(...) }}` macro.

vars:
  # In-line band half-width (percentage points) for the ahead/behind/in_line
  # label. A holding within this many pp of its benchmark reads "in_line".
  inline_band_pp: 2

  # Dead-band half-widths (percentage points) for the macro regime states.
  # A dimension's 3-month delta within +/- threshold reads as the neutral
  # middle state; beyond it, rising/falling. Tunable, not baked into SQL.
  regime_rate_threshold_pp: 0.25       # fed funds: ~one 25bp move
  regime_inflation_threshold_pp: 0.3   # change in YoY inflation rate
  regime_labor_threshold_pp: 0.2       # change in unemployment rate

  # Sector <-> rates co-movement: trailing window for the correlation, and the
  # |corr| dead-band for the moves-with / moves-against / rate-neutral label.
  comovement_window_days: 60           # ~3 months, matches the regime horizon
  comovement_threshold: 0.2

  # Trailing window (months) for the macro indicator sparklines.
  macro_trend_months: 12

on-run-start:
  - "{{ assert_portfolio_isolation() }}"

# Custom schemas are honored in prod only (see macros/generate_schema_name.sql).
# In dev everything collapses into the personal sandbox dataset; in prod these
# route to anchor_staging / anchor_intermediate / anchor_marts — the named
# contract the dashboard and scheduled builds read.
models:
  anchor:
    staging:
      +materialized: view
      +schema: anchor_staging
    intermediate:
      +materialized: view
      +schema: anchor_intermediate
    marts:
      +materialized: table
      +schema: anchor_marts

seeds:
  anchor:
    +schema: anchor_seeds
